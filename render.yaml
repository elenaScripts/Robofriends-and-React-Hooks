services:
  - type: web
    name: robofriends
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s build -l 10000
    envVars:
      - key: NODE_VERSION
        value: 18.18.0

